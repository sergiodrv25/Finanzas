# Copia este archivo a .env y rellena con los valores de tu proyecto Supabase
# (Dashboard > Project Settings > API). En Vercel, añádelas como
# Environment Variables del proyecto.
# Si no configuras nada, la app funciona en modo local (datos en el dispositivo).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
